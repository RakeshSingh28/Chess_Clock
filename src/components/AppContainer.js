import AppHeader from "./Header";
import AppBody from "./Body";

const AppContainer = () => {
  return (
    <div>
      <AppHeader />
      <AppBody />
    </div>
  );
};

export default AppContainer;
